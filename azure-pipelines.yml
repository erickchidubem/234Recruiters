# Node.js with Angular
# Build a Node.js project that uses Angular.
# Add steps that analyze code, save build artifacts, deploy, and more:
# https://docs.microsoft.com/azure/devops/pipelines/languages/javascript

trigger:
- project-completion

pool:
  vmImage: ubuntu-latest

steps:
- task: NodeTool@0
  inputs:
    versionSpec: '10.x'
  displayName: 'Install Node.js'

- script: |
    npm install -g @angular/cli
    npm install
    ng build --prod
  displayName: 'npm install and build'
- task: CopyPublishBuildArtifacts@1  
  displayName: 'Copy Publish Artifact: test'  
  inputs:  
    CopyRoot: dist  
    Contents: '**'  
    ArtifactName: test  
    ArtifactType: Container  
  
- task: DownloadPipelineArtifact@2  
  displayName: 'Download Pipeline Artifact'  
  inputs:  
    targetPath: ' $(Build.ArtifactStagingDirectory)/dist/234Recruiters'

- task: FtpUpload@2
  inputs:
    credentialsOption: 'inputs'
    serverUrl: 'ftp://win5241.site4now.net'
    username: 'gpiljobs-001'
    password: 'w2E4F5pxDGiM5AB'
    rootDirectory: '$(Build.ArtifactStagingDirectory)/dist/234Recruiters/test'
    filePatterns: '**'
    remoteDirectory: '/app' # '/wwwroot/$(Build.BuildId)/' h:\root\home\gpiljobs-001\www\shelterapptest
    clean: false
    cleanContents: false
    preservePaths: true
    trustSSL: false
  displayName: 'Uploading the 234Recruiters to FTP Server'